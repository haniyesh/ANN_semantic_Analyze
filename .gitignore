.env
.venv/
.venv311/
__pycache__/
*.pyc
*.npy
*.npz
*.pt
*.pkl
!xgb_feature_scaler_bert_rag.pkl
*.Zone.Identifier
*:Zone.Identifier

# Node
node_modules/
dashboard2/node_modules/
dashboard/node_modules/

# Data files (large / generated)
*.csv
*.json.bak
storage/news_cache.json
fear_greed_cache.json

# XGBoost binary model files (large artifacts — share via release/Drive)
xgb_impact_clf_*.json
!xgb_impact_clf_15m_bert_rag.json
xgb_price_reg_*.json

# Telegram session files (sensitive credentials)
*.session
my_session.session
telegram_session.session

# Research paper / LaTeX (not part of the codebase)
*.tex
*.aux
*.out
*.pdf

# Logs
*.log

# IDE
.vscode/

# Dashboard build output
dashboard2/dist/

# Groq sentiment cache (large, regenerable via build_groq_csv.py)
groq_sentiment_cache.json

# Results JSONs are intentionally tracked — main.py and the API read
# thresholds and metrics from them at startup.
# xgb_bert_results.json, xgb_groq_results.json, ann_*_results.json → committed
